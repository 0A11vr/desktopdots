#
# wm independent hotkeys
#
#mod1 + ctrl + p
#	sleep 0.2; \
#	scrot -a $(slop -f '%x,%y,%w,%h') '/tmp/%F_%T_$wx$h.png' -e 'xclip -selection clipboard -target image/png -i $f && rm $f'
#	$HOME/bin/sel_screenshot.sh
#	screenshot

#mod1 + p
#	$HOME/bin/screenshot.sh
#ctrl + Tab
#	skippy-xd-runner --toggle
#	rofi -show window
mod1 + ctrl + s
	rofi -show ssh --ssh -disable-history
# terminal emulator
#mod1 + Return
#	kitty

# program launcher
# mod1 + @space
# 	rofi -show drun
mod1 + ctrl + @space
	gmrun

# time
mod1 + d
#	$HOME/bin/gtime.sh

#mod3 + period
#	playerctl next --player=spotify
#mod3 + @Return
#	playerctl play-pause --player=spotify

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd


#mod3 + p
#	$HOME/bin/screenshotTEST select

mod3 + p
	$HOME/test
